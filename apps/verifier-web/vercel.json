{
  "buildCommand": "cd ../.. && npm install --legacy-peer-deps && npm run build --filter=verifier-web",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install --legacy-peer-deps",
  "ignoreCommand": "bash -c 'if [[ $VERCEL_GIT_COMMIT_REF == \"main\" ]] && [[ ! $(git diff HEAD^ HEAD --name-only | grep -E \"^(apps/verifier-web|packages)\") ]]; then exit 0; else exit 1; fi'"
}
